# Replit Server Environment Variables
# Copy these to Replit Secrets (lock icon on left sidebar)

JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=production

# PORT is automatically set by Replit, don't set it manually
